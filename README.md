Cette application permet de modéliser le comportement  d'une grille.
La grille de départ est donnée dans un fichier "input.txt" dans le package dataSource
la classe de lancement est : DeadOrAlive.java
